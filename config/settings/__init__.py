"""Settings package for DentaCRM.

Import the environment-specific module explicitly via
``DJANGO_SETTINGS_MODULE=config.settings.dev`` (or ``.prod``). This
package intentionally does NOT re-export anything so that a mistyped
settings module fails loudly instead of silently falling back.
"""
