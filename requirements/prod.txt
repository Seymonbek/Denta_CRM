# =============================================================================
# DentaCRM backend — production-only pinned requirements.
# =============================================================================
-r base.txt

# --- App server -------------------------------------------------------------
gunicorn==23.0.0

# --- Observability ----------------------------------------------------------
sentry-sdk==2.15.0
